import { combineReducers } from "redux";

// import your slices here
// import authReducer from "../features/auth/authSlice";

const rootReducer = combineReducers({
  // auth: authReducer,
  // ví dụ thêm slice khác ở đây
});

export default rootReducer;
